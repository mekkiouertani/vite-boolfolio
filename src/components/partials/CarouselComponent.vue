<template>
    <Carousel id="gallery" :items-to-show="1" :wrap-around="false" v-model="currentSlide">
        <Slide v-for="slide in 10" :key="slide" class="border">
            <div class="carousel__item">sopra {{ slide }}</div>
        </Slide>
    </Carousel>

    <Carousel id="thumbnails" :items-to-show="4" :wrap-around="true" v-model="currentSlide" ref="carousel">
        <Slide v-for="slide in 10" :key="slide">
            <div class="carousel__item border" @click="slideTo(slide - 1)">sotto {{ slide }}.</div>
        </Slide>
    </Carousel>
</template>
  

<script>
import { defineComponent } from 'vue';
import { Carousel, Slide } from 'vue3-carousel';
import 'vue3-carousel/dist/carousel.css';

export default {
    components: { Carousel, Slide, Navigation, },
    name: 'CarouselComponent',
    data() {
        return {
            currentSlide: 0,
        }
    },
    methods: {
        slideTo(val) {
            this.currentSlide = val
        },
    },
}
</script>

<style lang="scss" scoped>
#gallery {
    .carousel__item {
        height: 300px;
    }
}

#thumbnails {
    .carousel__item {
        min-height: 100px;
        cursor: pointer;
        min-width: 300px;
    }
}
</style>